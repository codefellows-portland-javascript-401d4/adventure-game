beginController.$inject = ['$scope'];

export default function beginController($scope) {


  $scope.message = 'Pick food or a gun.';
  // $scope.start = false;
  // $scope.message = 'You are on the trail.';

};
